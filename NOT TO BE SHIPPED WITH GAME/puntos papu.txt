    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end
    if not points[1] == "taken" then pac_points:draw(sprites, stage.positionsX[1], stage.positionsY[1], 0, actor_scaleX, actor_scaleY) end